# Daily Activity Report

DATE = [30.07.2026]
Daily_Note = Efforts were made to implement Java Agent files into the project to extract assertions in synchronization with runtime execution. Although multiple methods were attempted, the Java Agent files failed to produce the desired results. For dynamic slicing, the necessary assertions will be identified using the 'failing_tests' file—where Defects4J logs buggy test cases—thereby enabling the detection of incorrect statements.

---------------------------------------------------------------------------------------------------------------------------------------------------------------------

DATE = [18.08.2026]
Daily_Note = Investigated why Slicer4J's `-v <variable>` seed criterion frequently fails to resolve to a real Jimple local, by dumping and reading the actual Jimple output for a known case (Csv-13's `writer`/`printer`); found the root cause to be Soot's local-splitting on multiply-reassigned locals, not the `$` prefix originally suspected. Implemented a receiver-priority reordering fix for the existing `$stackN` retry mechanism. Implemented a control-dependence-ancestor fallback so ground-truth fault lines that are otherwise unanswerable (dead pure-deletion anchors) can instead be matched against the nearest executed guard/branch statement, rescuing 3 such lines across 2 bugs without changing any existing RQ CSV schema. Attempted an additional fix seeding an extra criterion on a call's return-value local (targeting Csv-3's `lexer.nextToken(...)` gap); verified it reached one interprocedural hop deeper but did not close the gap to the real fault line, while measurably increasing runtime and slightly worsening one ranking — reverted after direct comparison. Fixed an unrelated, independent bug in `common.upsert_csv_row` (a type mismatch silently prevented `rq5.csv` from de-duplicating rows across separate pipeline runs). Re-ran the full 4-target pipeline twice to check reproducibility: RQ0/RQ1/RQ4/RQ5 were fully reproducible; RQ2's hybrid slice-universe size showed ~0.5% run-to-run variance, traced to Soot's own documented multi-threaded instrumentation flakiness, not to any change made this session. Investigated a fourth fix (control-flow/omission-aware slicing for JacksonXml-1's wrong-branch bug) and determined it requires a substantially larger engineering effort (Slicer4J source changes or new runtime instrumentation) rather than a small logic change; deferred.

---------------------------------------------------------------------------------------------------------------------------------------------------------------------
